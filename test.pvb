_exit:
    push "exiting program"
    push 2
    pop rax
    exit

_pushall:
    push 100
    push 100
    push 100
    push 100
    push 100
    push 100
    push 100
    push 100

_printstack:
    print

_start:
    jne _pushall
    jmp _printstack
    pop rbx;
    mov rbx, rcx;
    push rcx
    push rdx
    mov ep, rax
    mov rcx, r10
    push 5
    print
    jmp _exit
